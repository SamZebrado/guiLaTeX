# Web 演示录制脚本

## 1. 开场介绍
- **录制目标**：介绍 Web 应用的整体情况
- **操作步骤**：
  1. 打开浏览器，加载 `web_prototype/index.html`
  2. 等待页面完全加载
  3. 展示整体布局
- **建议时长**：10秒
- **可以对外怎么说**："欢迎使用 guiLaTeX Web 应用，这是一个独立应用候选版，采用统一 UI 模式 v1"
- **不该说满的话**：无需特别说明限制
- **是否需要解释 bridge**：否

## 2. 统一 UI 模式展示
- **录制目标**：展示 Web 应用的统一 UI 布局
- **操作步骤**：
  1. 展示顶部主工具栏，介绍各个按钮功能
  2. 展示左/中画布区域
  3. 展示右侧固定属性面板
  4. 调整浏览器窗口大小，演示响应式设计
  5. 滚动右侧属性面板，展示滚动功能
- **建议时长**：20秒
- **可以对外怎么说**："Web 应用采用统一 UI 模式 v1，布局清晰，包括顶部主工具栏、左/中画布和右侧固定属性面板"
- **不该说满的话**：无需特别说明限制
- **是否需要解释 bridge**：否

## 3. 文本元素操作
- **录制目标**：演示文本元素的创建、编辑、拖动和调整大小
- **操作步骤**：
  1. 选择一个文本元素（如标题）
  2. 在右侧属性面板中编辑文本内容
  3. 拖动元素到新位置
  4. 调整元素大小
  5. 展示实时预览效果
- **建议时长**：25秒
- **可以对外怎么说**："文本元素操作流畅，支持实时编辑、拖动和大小调整"
- **不该说满的话**：无需特别说明限制
- **是否需要解释 bridge**：否

## 4. 多选与旋转功能
- **录制目标**：演示选择多个元素并进行批量旋转操作
- **操作步骤**：
  1. 点击 "多选模式" 复选框，启用多选模式
  2. 选择多个元素
  3. 在右侧属性面板中，使用旋转滑块调整旋转角度
  4. 展示所有选中元素同时旋转的效果
- **建议时长**：20秒
- **可以对外怎么说**："支持多选模式，可对多个元素进行批量旋转操作"
- **不该说满的话**：无需特别说明限制
- **是否需要解释 bridge**：否

## 5. 图层管理
- **录制目标**：演示元素的图层管理功能
- **操作步骤**：
  1. 选择一个元素
  2. 点击 "移到顶部" 按钮，展示元素移到最上层
  3. 选择另一个元素
  4. 点击 "移到底部" 按钮，展示元素移到最下层
  5. 演示 "上移" 和 "下移" 功能
- **建议时长**：15秒
- **可以对外怎么说**："支持完整的图层管理功能，可调整元素的前后顺序"
- **不该说满的话**：无需特别说明限制
- **是否需要解释 bridge**：否

## 6. 导出功能
- **录制目标**：演示导出 IR 和 PDF 功能
- **操作步骤**：
  1. 点击 "导出 IR" 按钮
  2. 展示下载的 JSON 文件
  3. 点击 "导出 PDF" 按钮
  4. 展示浏览器打印对话框
  5. 选择 "保存为 PDF" 选项
- **建议时长**：20秒
- **可以对外怎么说**："支持导出 IR 中间格式和 PDF 格式"
- **不该说满的话**：无需特别说明限制
- **是否需要解释 bridge**：否

## 7. LaTeX 导出（补充演示）
- **录制目标**：演示 LaTeX 导出流程
- **操作步骤**：
  1. 点击 "导出 LaTeX" 按钮
  2. 展示自动导出的 IR 文件
  3. 简要说明需要运行 bridge 脚本生成 LaTeX
  4. 展示生成的 LaTeX 文件示例
- **建议时长**：15秒
- **可以对外怎么说**："支持导出自家风格的 LaTeX 文件"
- **不该说满的话**："需要通过 Python bridge 脚本完成转换"
- **是否需要解释 bridge**：是，需要说明依赖 Python bridge 脚本

## 8. LaTeX 导入（补充演示）
- **录制目标**：演示 LaTeX 导入流程
- **操作步骤**：
  1. 简要说明需要运行 bridge 脚本将 LaTeX 转换为 JSON
  2. 点击 "打开项目" 按钮
  3. 选择生成的 JSON 文件
  4. 展示导入的内容
- **建议时长**：15秒
- **可以对外怎么说**："支持导入 conforming LaTeX 文件"
- **不该说满的话**："需要通过 Python bridge 脚本完成转换"
- **是否需要解释 bridge**：是，需要说明依赖 Python bridge 脚本

## 9. 项目保存和打开
- **录制目标**：演示项目保存和打开功能
- **操作步骤**：
  1. 点击 "保存项目" 按钮
  2. 展示下载的 JSON 文件
  3. 点击 "打开项目" 按钮
  4. 选择之前保存的 JSON 文件
  5. 展示加载的项目
- **建议时长**：15秒
- **可以对外怎么说**："支持项目的保存和打开功能，使用 JSON 格式存储"
- **不该说满的话**：无需特别说明限制
- **是否需要解释 bridge**：否

## 10. 结尾总结
- **录制目标**：总结 Web 应用的主要功能和特点
- **操作步骤**：
  1. 回到应用主界面
  2. 总结已演示的功能
  3. 说明 Web 应用的状态和未来规划
- **建议时长**：10秒
- **可以对外怎么说**："guiLaTeX Web 应用已经是独立应用候选版，统一 UI 模式 v1 稳定，Playwright regression 稳定，LaTeX 闭环已支持跨端路径"
- **不该说满的话**："LaTeX 功能依赖 Python bridge 脚本，部分高级字段暂未支持"
- **是否需要解释 bridge**：是，需要简要说明 LaTeX 功能依赖 bridge