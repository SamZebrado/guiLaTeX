# ExportCore 验证状态表

## 状态分类

### 1. 已真实验证

| 项目 | 验证状态 | 说明 |
|-----|---------|-----|
| 字段导出 | ✅ 已验证 | 所有 IR 字段都能正确导出到 LaTeX，字段类型和值的转换正确 |
| TeX 文件生成 | ✅ 已验证 | 成功生成可编译的 LaTeX 文件，生成的文件结构完整 |
| Golden sample 输出 | ✅ 已验证 | golden sample 成功生成 LaTeX |
| Regression sample 1 (Rotation) 输出 | ✅ 已验证 | 带 rotation 的文本/图片样例成功生成 LaTeX |
| Regression sample 2 (Layers) 输出 | ✅ 已验证 | 带 layer 差异的样例成功生成 LaTeX |
| Regression sample 3 (Same Layer) 输出 | ✅ 已验证 | 多对象同一 layer 的样例成功生成 LaTeX |
| Regression sample 4 (Fonts) 输出 | ✅ 已验证 | 中英文字体分离的样例成功生成 LaTeX |
| Regression sample 5 (Rotation + Layers) 输出 | ✅ 已验证 | rotation + layer 同时存在的样例成功生成 LaTeX |
| 图层顺序 | ✅ 已验证 | 按 layer 值升序渲染，数值越大层级越高，图层顺序在生成的 LaTeX 中正确体现 |

---

### 2. 设计上支持但未最终视觉实证

| 项目 | 当前状态 | 说明 |
|-----|---------|-----|
| 位置保真 | 设计已实现，需要最终 PDF 视觉验证 | 直接使用 IR 中的 x, y 坐标，单位为 mm，转换为 LaTeX 的绝对坐标系统。目标：误差 < 1mm |
| 尺寸保真 | 设计已实现，需要最终 PDF 视觉验证 | 直接使用 IR 中的 width, height，单位为 mm。目标：误差 < 1mm |
| 旋转保真 | 设计已实现，需要最终 PDF 视觉验证 | 直接使用 IR 中的 rotation，单位为度，正方向为顺时针。目标：误差 < 1度 |
| 文本框与文本内容的关系 | 部分实现，存在限制 | 文本换行可能与原设计有差异；行间距和字间距由 LaTeX 自动控制；文本超出文本框时不会自动裁剪 |
| 字体 fallback 后的差异风险 | 需要注意风险 | 字体 fallback 后可能导致文本尺寸变化；不同字体的字符宽度不同，可能影响整体布局 |

---

### 3. 当前仍有缺口

| 项目 | 缺口说明 | 优先级 |
|-----|---------|------|
| 文本布局差异 | 文本换行、行间距、字间距与原设计有差异 | 高 |
| 字体 fallback 风险 | 字体不可用时导致文本尺寸和布局变化 | 高 |
| 旋转精度 | 旋转角度可能存在微小误差 | 中 |
| 文本框边界处理 | 文本超出文本框时不会自动裁剪 | 中 |
| 页面边距处理 | 页面边距可能与原设计有差异 | 低 |
| Web 端 font_family_zh / font_family_en 字段 | 需要将现有 fontFamily 字段拆分为中英文分离的两个字段 | 高 |
| Web 端 page 字段 | 需要添加 page 字段支持 | 中 |
| Qt 端 font_family_zh / font_family_en 字段 | 需要将现有 font_family 字段拆分为中英文分离的两个字段 | 高 |

---

## 重要声明

⚠️ **请勿过度宣称**

1. **不要**把"tex 文件生成成功"直接写成"几何一比一已验证"
2. **不要**把"设计已实现"直接写成"已完全验证"
3. **请保持**表述收敛，只宣称已真实验证的内容
4. **请明确区分**：
   - 已真实验证（tex 生成、字段导出、样例输出等）
   - 设计上支持但未最终视觉实证（位置、尺寸、旋转的视觉保真度）
   - 当前仍有缺口（明确列出的缺口项）

## 对"几何一比一"影响最大的 3-5 个缺口

1. **文本布局差异**：
   - 影响：文本换行、行间距、字间距与原设计有差异
   - 原因：LaTeX 文本布局算法与 GUI 不同
   - 优先级：高

2. **字体 fallback 风险**：
   - 影响：字体不可用时导致文本尺寸和布局变化
   - 原因：不同字体的字符宽度和渲染方式不同
   - 优先级：高

3. **旋转精度**：
   - 影响：旋转角度可能存在微小误差
   - 原因：LaTeX 旋转实现与 GUI 实现的差异
   - 优先级：中

4. **文本框边界处理**：
   - 影响：文本超出文本框时不会自动裁剪
   - 原因：LaTeX 文本框处理机制限制
   - 优先级：中

5. **页面边距处理**：
   - 影响：页面边距可能与原设计有差异
   - 原因：LaTeX 页面布局机制与 GUI 不同
   - 优先级：低
