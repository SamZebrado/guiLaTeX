# 外务大臣奏报：公开发布最终整理完成

太师钧鉴：

臣外务大臣已按指令完成公开发布最终整理，以下为汇报。

---

## 1. 本次发布整理采用了什么原则

**保留开发痕迹，只清隐私。** 不因"像内部文档"就机械删除，通过阅读导引（`docs/README_FOR_HUMANS.md`）分层而非大面积清空。

## 2. 删除了哪些内容

**本轮零删除。** 之前几轮已清理完毕：
- `agent_backup/`、`trae_multi_agent_ram/`、`temp/`、`venv/` — 整体排除
- `chatgpt`/`openai` 字样 — 已替换
- `playwright-plugins.md` — 含外部项目名，已移除
- `__MACOSX/`、`._*`、`.DS_Store` — 已清理

## 3. 保留了哪些原本可能会被删的内容

按旧思路可能会被删、但按新偏好保留的：
- `docs/audits/` — 内部审计文档（SOLO/MTC/libEGL 痕迹）
- `docs/skills/` — Agent 行为规范
- `PLAN.md`、`PROJECT_LOG.md`、`RUN_LEDGER.md`、`MULTI_AGENT_PLAN.md`
- `docs/contest_evidence/dev_journal.md` — 开发日志
- `整活_争储朝堂/` — 项目文化材料
- 全部历史中间版本截图（web_test_*、web_regression_v1/v3）
- `showcase/index.html` — 宫廷风开发展示页

## 4. 新增/更新了哪份"给人类读者的说明"

**`docs/README_FOR_HUMANS.md`**（已存在，本轮确认内容完整）
- 分 6 层：先读 / Agent 相关 / 审计复盘 / 测试证据 / 项目文化 / 研究
- 标注了证据性质（真实 v4 vs 历史中间版本 vs Qt headless 证据）
- 标注了 Qt 线当前状态

## 5. 更新了哪个"发布偏好"文档

**`RELEASE_SANITIZATION_RULES.md`**（主目录，非发布版内）
- 第零节：发布偏好（保留开发痕迹，只清隐私）
- 同步规则：每次整理必须同步主目录 git 已追踪变更，同步后必须重新隐私扫描

## 6. 当前公开版最准确的一句话表述

> Web-first v1 里程碑版：Web + Core 是当前主可用路径（支持 IR/JSON 导出、LaTeX 生成、浏览器打印路径 PDF 导出），Qt 仍在独立 clean venv 中推进，不作为当前版本完成判据。保留完整 AI Agent 协作开发过程痕迹。

## 7. 是否已 commit

**是**，最新 commit：`988ab06`（add showcase page and sync latest changes）
共 8 个 commit，`main` 分支。本轮同步后 working tree clean，无新变更需提交。

## 8. 是否建议 push 到 GitHub

**Yes，建议直接 push。** 隐私扫描零匹配，口径一致，阅读说明和展示页已就位。

```bash
cd release_public && git push
```

---

*臣外务大臣叩首。展示页已备好，网友点进来看便是。* 📜

*太师若还有差遣，臣随时听令。*
